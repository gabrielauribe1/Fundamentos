#include<stdio.h>
int main (void)
{
  int num, cont=0;
    do
      {
	printf("introduzca numero, cero para terminar: ");
	scanf("%d",&num);
	if(num!=0)
	  {
	    printf("%d",num);
	    cont=cont+1;
	  }
      }
	while(num!=0);
	printf("se introdujeron %d numeros\n", cont);
}

